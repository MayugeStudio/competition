#include <iostream>
#include <vector>
#include <queue>
using namespace std;
using ll = long long;


int main() {
  ll n, m; cin >> n >> m;
  vector<ll> a(n);
  vector<ll> b(n);
  vector<ll> d(n);
  for (int i=0; i<n; ++i) cin >> a[i];

  for (int i=0; i<n; ++i) {
  }

  for (int i=0; i<m; ++i) {
    ll u, v; cin >> u >> v >> b[i];

  }

  return 0;
}
