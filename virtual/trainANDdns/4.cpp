#include <iostream>
#include <map>
#include <set>
#include <vector>
using namespace std;


int main() {
  int n; cin >> n;
  vector<int> a(n);
  set<int> k;
  for (int i=0; i<n; ++i) {
    cin >> a[i];
    k.insert(a[i]);
  }
  map<int, string> m; 
  for (int i=0; i<n; ++i) {
    string s;
    for (int i=0 i<n; ++i) {
      if (a[i] == )
    }
    m[i];
      
  }

  return 0;
}
